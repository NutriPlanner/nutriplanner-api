const Joi = require('joi');
const { objectId } = require('./custom.validation');

const create{{pascalCase name}} = {
  body: Joi.object().keys({
  }),
};

const get{{pascalCase name}}Page = {
  query: Joi.object().keys({
    filter: Joi.string(),
    sortBy: Joi.string().allow(''),
    limit: Joi.number().integer(),
    page: Joi.number().integer(),
  }),
};

const update{{pascalCase name}} = {
  params: Joi.object().keys({
    {{camelCase name}}Id: Joi.required().custom(objectId),
  }),
  body: Joi.object().keys({
  }),
};

const delete{{pascalCase name}} = {
  params: Joi.object().keys({
    {{camelCase name}}Id: Joi.string().custom(objectId),
  }),
};

module.exports = {
  create{{pascalCase name}},
  get{{pascalCase name}}Page,
  update{{pascalCase name}},
  delete{{pascalCase name}},
};
